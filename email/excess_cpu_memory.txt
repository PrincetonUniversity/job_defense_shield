<GREETING>

Below are <CASE> that ran on HPC Cluster (<PARTITIONS>) during the past <DAYS> days:

<TABLE>

It appears that you are requesting more CPU memory than your jobs require. On average, these jobs used only <PERCENT> of the allocated memory across <NUM-JOBS> jobs.

This resulted in <UNUSED> TB-hours of unused memory, reducing the amount of memory available to other jobs on the cluster.

A good target value for Percent-Used is 80% or higher. Please consider requesting less memory by adjusting the --mem-per-cpu or --mem Slurm directive.

You can review memory utilization using JobStats. For example:

<JOBSTATS>

For graphical job metrics and historical utilization data, use RCC JobStats through Open OnDemand:
https://ondemand.rcc.mcw.edu/pun/sys/jobstats

For guidance on requesting and using cluster resources efficiently, see:
https://docs.rcc.mcw.edu/cluster/jobs/running-jobs

If you need assistance determining appropriate memory requirements, simply reply to this email. Your reply will automatically create a Research Computing Center support ticket.
