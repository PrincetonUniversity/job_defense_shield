<GREETING>

Over the last <DAYS> days, you have used <RANK> CPU-hours on HPC Cluster (<PARTITIONS>), but your mean CPU efficiency was only <EFFICIENCY>:

<TABLE>

A good target value for CPU efficiency is <TARGET> or higher.

Low CPU utilization often indicates one or more of the following:

  - The application is using fewer CPU cores than were requested.
  - The application is waiting on data or external processes.
  - More CPU cores were requested than the application can effectively use.
  - An interactive session was allocated but spent significant time idle.

You can review detailed job metrics, including CPU utilization, memory usage, and job timelines using RCC JobStats:

Command-line:
<JOBSTATS>

Graphical JobStats dashboard with CPU, memory, GPU, and runtime utilization charts:
<JOBSTATSURL>

For guidance on requesting and using cluster resources efficiently, see:
https://docs.rcc.mcw.edu/cluster/jobs/running-jobs

Reviewing your application's documentation may help identify configuration settings that improve CPU utilization.

If you would like assistance interpreting JobStats output or improving performance, simply reply to this email. Your reply will automatically create a Research Computing Center support ticket.
