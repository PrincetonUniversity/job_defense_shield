<GREETING>

Over the last <DAYS> days, you have used <RANK> GPU-hours on HPC Cluster (<PARTITIONS>), but your mean GPU efficiency was only <EFFICIENCY>:

<TABLE>

A good target value for GPU efficiency is <TARGET> or higher.

Low GPU utilization often indicates one or more of the following:

  - The GPU is waiting on CPU processing.
  - The workload is too small to efficiently utilize the allocated GPU resources.
  - The application is not configured to fully utilize the GPU.
  - An interactive session was allocated but spent significant time idle.

You can review detailed job metrics, including GPU utilization, CPU utilization, memory usage, and job timelines using RCC JobStats.

Command-line:
<JOBSTATS>

Graphical JobStats dashboard with CPU, memory, GPU, and runtime utilization charts:
<JOBSTATSURL>

For guidance on requesting and using cluster resources efficiently, see:
https://docs.rcc.mcw.edu/cluster/jobs/running-jobs

Reviewing your application's documentation may help identify configuration settings that improve GPU utilization.

If you would like assistance interpreting JobStats output or improving performance, simply reply to this email. Your reply will automatically create a Research Computing Center support ticket.
