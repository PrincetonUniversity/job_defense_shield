<GREETING>

Below are <CASE> that ran on HPC Cluster (<PARTITIONS>) during the past <DAYS> days:

<TABLE>

The CPU utilization pattern of these jobs suggests that the application may be using fewer CPU cores than were allocated.

In some cases this is expected, particularly when additional CPU cores are requested to obtain sufficient memory for the workload. If this is not the case, consider reviewing your resource requests and application configuration.

You can review CPU utilization using JobStats. For example:

<JOBSTATS>

For guidance on requesting and using cluster resources efficiently, see:
https://docs.rcc.mcw.edu/cluster/jobs/running-jobs

If you need assistance determining the appropriate CPU and memory resource requests for your workloads, simply reply to this email. Your reply will automatically create a Research Computing Center support ticket.
