<GREETING>

Below are your recent jobs that showed zero GPU utilization on one or more allocated GPUs:

<TABLE>

The GPU utilization was 0% on one or more allocated GPUs. A total of <GPU-HOURS> GPU-hours were consumed at 0% utilization during the past <DAYS> days.

It is your responsibility to ensure that the GPUs and other resources are being
used efficiently. Please monitor your jobs using the "jobstats" command:

Command-line:
<JOBSTATS>

Graphical JobStats dashboard with CPU, memory, GPU, and runtime utilization charts:
<JOBSTATSURL>

For guidance on requesting and using GPU resources efficiently, see:
https://docs.rcc.mcw.edu/cluster/jobs/running-jobs

Common causes of zero GPU utilization include:

  - The application was not configured to use the allocated GPU(s).
  - Required GPU libraries or modules were not loaded.
  - The application failed early and never reached GPU execution.
  - CPU-only code was executed on a GPU allocation.

Please investigate this issue before submitting additional GPU jobs.

If you need assistance, simply reply to this email. Your reply will automatically create a Research Computing Center support ticket.
